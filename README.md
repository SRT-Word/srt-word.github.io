# SRT ⇄ Script Compare

Инструмент для сверки SRT-субтитров с утверждённым сценарием (Word) и сборки
исправленного SRT-файла. **Тайм-коды и разбиение на блоки остаются нетронутыми —
меняется только текст.**

Заменяет ручной процесс «скопировать текст из SRT → почистить от тайм-кодов →
сравнить на anytexteditor.com → вручную перенести правки в SRT» одним экраном:
вставили два текста → нажали «Сравнить» → кликами выбрали нужные правки →
скачали готовый SRT.

## Как пользоваться

1. Откройте `index.html` в браузере (или опубликуйте на GitHub Pages — см. ниже).
2. **Слева** вставьте содержимое SRT-файла целиком (с номерами и тайм-кодами)
   или загрузите `.srt`.
3. **Справа** загрузите `.docx` или `.doc` со сценарием — текст извлечётся
   автоматически:
   - если документ — мастер-таблица со столбцом **Script** (#, Section, Script,
     Asset…), берётся только этот столбец, остальное игнорируется;
   - зачёркнутые фрагменты отбрасываются;
   - непринятые правки рецензирования (tracked changes) обрабатываются как
     принятые: удалённое отбрасывается, вставленное остаётся;
   - маркированные списки получают «•».

   Можно и вставить текст вручную, но тогда сначала примите/отклоните правки
   рецензирования в самом Word — при копировании удалённый текст попадает в
   буфер обмена вместе с обычным.

   Старый формат **.doc** (Word 97-2003) читается напрямую, без конвертации:
   разбираются контейнер OLE2, поток `WordDocument` и таблица кусков (piece
   table), таблицы восстанавливаются по меткам конца ячейки/строки, а
   зачёркнутый текст и правки рецензирования определяются по свойствам
   символов (CHPX). Единственное отличие от `.docx`: маркеры списков «•» в
   `.doc` не хранятся в тексте — их придётся добавить вручную.
4. Нажмите **«Сравнить»**. Ниже появится текст с подсветкой:
   - красное зачёркнутое — убирается из SRT;
   - зелёное — добавляется из сценария;
   - серые метки `12` — номера блоков субтитров, чтобы быстро найти место.
5. **Клик по любой подсветке включает/выключает правку.** Это важно: сценарий
   не всегда прав (номера моделей, URL, телефоны, реально произнесённые слова
   чаще правильнее в SRT) — такие правки просто выключите.
6. Готовый SRT формируется в нижнем поле автоматически. **«Скачать .srt»** —
   и файл готов (имя: `исходное_имя_CORRECTED.srt`).

## Правила, заложенные в инструмент

Выведены из практики сверки обучающих видео (Harley-Davidson LMS):

- Тайм-коды не изменяются никогда; нумерация блоков в результате идёт заново с 1.
- «Умные» кавычки/апострофы (’ “ ”), неразрывные пробелы и символ «…» не
  считаются различиями — в результате остаётся вариант написания из SRT.
- Зачёркнутый текст в `.docx` = вырезанные из видео фрагменты, отбрасывается
  при импорте.
- Непринятые правки рецензирования Word (tracked changes) при загрузке `.docx`
  обрабатываются автоматически: tracked-удаления отбрасываются, tracked-вставки
  принимаются (поведение проверено тестами).
- Из мастер-таблиц берётся только столбец «Script»; прочие колонки (#, Section,
  Asset, Key Reference) и служебные таблицы (CHARTER и т.п.) игнорируются.
- Маркированные списки из `.docx` переносятся с маркером «•»; маркер на границе
  блоков субтитров прикрепляется к следующему блоку.
- Одиночные тире сценария («--», «—», «–») превращаются в « - ». Оторванный
  дефис («De Dion -style») отделяется от слова.
- **Текст не переезжает в чужой тайм-код.** Заменяющие слова распределяются по
  блокам выравниванием по схожести с заменяемыми (Нидлман-Вунш), а не подряд:
  «De Dion» встаёт вместо «D-D-On» в своём блоке, «style» остаётся в следующем.
- **Пунктуационные коллизии** после вырезок в доке исправляются: «. ,» и «, .»
  невозможны — остаётся один знак (перед строчной буквой запятая, перед
  заглавной точка). Троеточие «...» не трогается.
- **Вырезанный текст не склеивает соседние слова:**
  «providing~~ precise ~~optimal» → «providing optimal».
- **Перенос строки внутри абзаца Word** (Shift+Enter, в HTML `<br>`) не
  склеивает предложения: «score.It» → «score. It», «verbatim.Use» →
  «verbatim. Use». Домены (H-D.com) и аббревиатуры (H.O.G.) не затрагиваются.
- Ссылки на комментарии Word (`[[ZD1]](#_msocom_1)`), строки с их содержимым
  и markdown-зачёркивание `~~...~~` удаляются из вставленного текста.
- Длинные фрагменты (8+ слов), которые есть в SRT, но отсутствуют в сценарии
  (вступления, представления спикеров), по умолчанию **не** удаляются —
  включите такую правку кликом, если удалить всё же нужно.
- Переносы строк внутри блока сохраняются на исходных позициях; заменяющие
  слова наследуют строку заменяемых. Если из-за правок строка «слиплась»,
  блок автоматически перебивается на исходное число строк.
- Пустые блоки (в т.ч. «хвостовые» с нулевой длительностью) удаляются из
  результата (отключается галочкой).

## Публикация на GitHub Pages

1. Создайте репозиторий на GitHub и загрузите в него содержимое этой папки
   (файл `index.html` должен лежать в корне).
2. В репозитории: **Settings → Pages → Source: Deploy from a branch →
   Branch: `main`, папка `/ (root)` → Save**.
3. Через минуту сайт будет доступен по адресу
   `https://<ваш-логин>.github.io/<имя-репозитория>/`.

Можно и без GitHub: просто откройте `index.html` локально двойным кликом.

## Приватность

Всё работает в браузере, никакие файлы никуда не отправляются. Интернет нужен
только один раз — чтобы подгрузить библиотеку
[mammoth.js](https://github.com/mwilliamson/mammoth.js) (чтение `.docx`) с CDN.
Если открыть страницу без интернета, всё будет работать, кроме загрузки
`.docx` — текст сценария можно вставить вручную.

## Тесты

```bash
node tests/test.js                          # логика сверки и сборки SRT
cd tests && npm i mammoth linkedom          # один раз, для чтения документов
node docx-test.js  [мастер-док.docx]        # извлечение из .docx
node doc-legacy-test.js [файл.doc]          # чтение Word 97-2003
```

Проверяются: разбор SRT, дифф, привязка замен к блокам и строкам, буллиты на
границах блоков, поведение длинных удалений, удаление пустых блоков,
перенумерация, пунктуационные коллизии, склейки предложений, извлечение
колонки Script из .docx и .doc, зачёркнутый текст и правки рецензирования.

## Структура

```
index.html            — страница приложения
css/style.css         — стили
js/app.js             — логика (парсер SRT, дифф Майерса по словам, сборка результата)
js/doc.js             — чтение старого формата .doc (OLE2 + piece table + CHPX)
tests/                — тесты для Node.js (см. tests/README.txt)
favicon.svg           — векторная фавиконка «торнадо» (+ .ico, png 16/32/48/192/512,
                        apple-touch-icon.png)
```

## Лицензия

MIT

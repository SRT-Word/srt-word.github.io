Тесты (нужен Node.js)

  node test.js
      Базовая логика: разбор SRT, дифф, привязка правок к блокам и строкам,
      пунктуационные коллизии, склейки предложений, тире, буллиты.

  npm i mammoth linkedom      (один раз, для тестов чтения документов)

  node docx-test.js [путь_к_мастер-доку.docx]
      Извлечение из .docx: колонка Script, правки рецензирования,
      Shift+Enter, зачёркнутый текст. Без аргумента — только синтетика.

  node doc-legacy-test.js [путь_к_файлу.doc]
      Чтение старого формата .doc (Word 97-2003): подбор структуры таблицы,
      зачёркнутый текст и правки рецензирования. Без аргумента — синтетика
      и фикстура fixtures/tracked.doc.

fixtures/
  tracked.docx    — .docx с правками рецензирования и зачёркиванием
  linebreak.docx  — .docx с Shift+Enter внутри ячейки
  tracked.doc     — тот же документ в формате Word 97-2003
